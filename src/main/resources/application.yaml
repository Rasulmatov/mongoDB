spring:
  datasource:
    url: jdbc:h2:mem:~
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  h2:
      console:
        enabled: true
        path: /h2-console
  graphql:
    graphiql:
      enabled: true
      path: /graphiql
      endpoint: /graphql
logging:
  level:
    org.jline: DEBUG   # Bu to'g'ri formatda
    root: info





